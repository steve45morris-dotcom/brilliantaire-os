node_modules/
dist/
.DS_Store
*.log
.env*
.env.local
.mcp.local.json
mcp_secrets/
scratch/



# Audio and Piper Staging Cache
local_assets/
outputs/narrator/tts_queue/models/
outputs/narrator/tts_queue/rendered_audio/
outputs/narrator/tts_queue/bin/
*.wav
*.mp3
*.tar.gz
test.wav

# Coldspark telemetry — derived from shell history, may capture secrets.
# Trailing glob is deliberate: it must also cover -wal/-shm sidecars and
# .bak/.orig snapshots, which carry the same content as the database itself.
data/*.db*
