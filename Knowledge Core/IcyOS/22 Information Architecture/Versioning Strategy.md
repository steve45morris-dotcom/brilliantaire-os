# 📈 Versioning Strategy
`Version: 1.0.0` | `Status: Active` | `Scope: Information Architecture`

Data versioning schemas and schema migration version increments.

- **Entity versioning**: Entities tracking modifications must use incremental `version` integers (e.g. `version: 1`, `version: 2`) and timestamps.
- **Git Versioning**: Locked on annotated tag baselines.

---

## 📋 Document Metadata
- **Purpose**: Record versioning strategies.
- **Version**: 1.0.0

*I build before burning.*
