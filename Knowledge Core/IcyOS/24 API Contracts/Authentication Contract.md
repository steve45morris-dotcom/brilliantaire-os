# 🔐 Authentication Contract
`Version: 1.0.0` | `Status: Active` | `Scope: API Security`

Defines token and auth formats.

- **Supabase Auth**: Utilizes JWT bearers (`Authorization: Bearer <token>`).
- **User Claims**: Sub claims map directly to `users.id` (UUIDv4 format).

---

## 📋 Document Metadata
- **Purpose**: Record auth token standards.
- **Version**: 1.0.0

*I build before burning.*
