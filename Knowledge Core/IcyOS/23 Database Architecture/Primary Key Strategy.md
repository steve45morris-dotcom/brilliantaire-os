# 🔑 Primary Key Strategy
`Version: 1.0.0` | `Status: Active` | `Scope: Database Architecture`

Primary key strategies to ensure data isolation.

- **UUIDv4**: All tables use `UUID` data type keys initialized via `uuid_generate_v4()`.
- **Obsidian wikilinks slug identifier mappings**: Mapped directly to UUID database identifiers.

---

## 📋 Document Metadata
- **Purpose**: Record primary key strategies.
- **Version**: 1.0.0

*I build before burning.*
