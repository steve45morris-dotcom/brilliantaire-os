# 🚦 Decision Hierarchy
Check deterministic local rules. Route inbox prompts. If high confidence target match exists, execute locally. Else, escalate to AI Runtime capability profiles (fast, reasoning, heavy).
